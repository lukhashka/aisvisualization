import { activations } from '../../lib/neuron';

export function Theory() {
  return (
    <div className="theory">
      <p>
        <strong>Формальний нейрон Мак-Каллока — Піттса</strong> обчислює зважену суму входів,
        додає зміщення й пропускає результат через функцію активації:
      </p>
      <div className="formula" style={{ margin: '10px 0' }}>
        net = w₁·x₁ + w₂·x₂ + b &nbsp;&nbsp;→&nbsp;&nbsp; y = f(net)
      </div>
      <p>
        <strong>Ваги</strong> w₁, w₂ визначають, наскільки сильно кожен вхід впливає на результат:
        додатна вага «за», від'ємна — «проти». <strong>Зміщення</strong> b відповідає порогу
        спрацювання: нейрон видає одиницю, коли net ≥ 0, тобто коли w₁x₁ + w₂x₂ ≥ −b = θ.
      </p>
      <p>
        Рівняння <span className="mono">w₁x₁ + w₂x₂ + b = 0</span> задає на площині входів
        пряму — <strong>розділювальну гіперплощину</strong>. Усе, що з одного її боку, нейрон
        відносить до класу 1, з іншого — до класу 0. Саме тому один нейрон може реалізувати лише{' '}
        <strong>лінійно роздільні</strong> функції: AND, OR, NOT, NAND, NOR.
      </p>
      <p>
        Для <strong>XOR</strong> потрібні набори (0,1) і (1,0) в одному класі, а (0,0) і (1,1) — в
        іншому. Жодна пряма так їх не розділить, тому XOR розв'язується лише багатошаровою
        мережею (наприклад, XOR = (x₁ OR x₂) AND NAND(x₁, x₂)).
      </p>
      <h3 style={{ marginTop: '1.4em' }}>Навіщо чотири функції активації</h3>
      <p>
        Функція активації вирішує, <strong>як саме</strong> нейрон перетворює зважену суму net
        на вихід. Для самих логічних функцій вистачило б однієї — порогової. Решта три додані,
        щоб було видно, <strong>чому в реальних мережах від неї відмовились</strong>: кожна
        наступна прибирає недолік попередньої.
      </p>

      <table className="ttable ttable--left" style={{ margin: '10px 0 14px' }}>
        <thead>
          <tr>
            <th>Функція</th>
            <th>Вихід</th>
            <th>Навіщо потрібна</th>
          </tr>
        </thead>
        <tbody>
          {Object.values(activations).map((a) => (
            <tr key={a.id} style={{ cursor: 'default' }}>
              <td>
                <strong>{a.label}</strong>
                <div className="mono" style={{ fontSize: '.8rem', color: 'var(--text-dim)' }}>
                  {a.formula}
                </div>
              </td>
              <td className="mono" style={{ whiteSpace: 'nowrap' }}>{a.outputRange}</td>
              <td>{a.purpose}</td>
            </tr>
          ))}
        </tbody>
      </table>

      <p>
        Головне, що варто перевірити самому: перемкни активацію, не змінюючи ваг, — і подивись
        на площину входів. <strong>Розділювальна пряма майже не рухається</strong>, бо її задають
        самі ваги через рівняння net = const, а не форма активації. Змінюється лише{' '}
        <em>поверхня</em> над цією площиною: у порогової це різка «сходинка», у сигмоїди й tanh —
        плавний схил, у ReLU — злам і нескінченне зростання. Тобто активація визначає не{' '}
        <em>де</em> проходить межа, а наскільки різко нейрон біля неї «перемикається».
      </p>
      <p>
        Виняток — <strong>ReLU</strong>: її вихід не обмежений зверху, тому логічною одиницею
        вважається y ≥ 0.5, а це відповідає net ≥ 0.5, а не net ≥ 0. Через це її розділювальна
        пряма зсунута відносно решти трьох (на графіку це видно).
      </p>
      <p>
        Параметр <strong>k</strong> у сигмоїди й tanh керує крутизною: при великих k вони
        практично збігаються з пороговою функцією, при малих — нейрон стає «невпевненим» і
        видає щось середнє між 0 і 1. Це добре видно на 3D-графіку.
      </p>
    </div>
  );
}
